//cin.ignore() after cin>>w;
#include<iostream>
using namespace std;

int main()
{
    char w[100];
    cin>>w;
    cout<<w;
}
//in output, cursor is in new line after it prints w, meaning that '\n' character is saved as the last character