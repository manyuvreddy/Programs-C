// finding area of circle wih if function
#include <stdio.h>
#define pi 3.1415
int main() {
    float Area;
    float a=0,b=0;
    printf("enter radius \n");
    scanf("%f",&a);
    Area=pi*a*a;
    if (a>0)
    printf("area is %f", Area);
    else 
    printf("the radius is negative");

    return 0;
}
